name: Deploy on self hosted runner 

on:
    workflow_dispatch :

jobs: 
  deploy:
    runs-on: self-hosted


    steps:
      - name: checkout code
        uses: actions/checkout@v6

      - name: Start services
        run: docker compose up -d --build
